node_modules/
dist/
.wrangler/
.dev.vars
*.local
.DS_Store

# Offline official-account root keys must never enter source control. The
# provisioning CLI also refuses to create/read them anywhere inside this repo.
**/skytale-admin-root*.json
**/official-admin-root*.json
__pycache__/

# Local-only German copy of SECURITY.md (English is the committed source of truth)
SECURITY.de.md

# Local-only point-in-time security audit reports (never committed)
SECURITY_AUDIT_*.md

# Scratch/intermediate build artifacts (e.g. the decoy content JSON baked into decoyContent.ts)
scratchpad/

# Large boot-splash SOURCE assets — the optimized in-app copy lives in src/assets/bootSplash.json.
# Never commit the raw multi-MB media (a 41 MB .mov etc.) into git.
docs/*.mov
docs/*.mp4
docs/rod animation.json
